[wordpress]

listen = /run/php/php7.4-fpm.sock

listen.owner = www-data
listen.group = www-data

user = www-data
group = www-data

pm = ondemand
pm.max_children = 10
pm.process_idle_timeout = 60s
pm.max_requests = 500
